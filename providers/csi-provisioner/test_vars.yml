---

# Test specific parameters

test_name: "csi-provision"

cspc: "{{ lookup('env','CSPC') }}"

storage_class: "{{ lookup('env','PROVIDER_STORAGE_CLASS') }}"

replica_count: "{{ lookup('env','REPLICA_COUNT') }}"

node_os: "{{ lookup('env','NODE_OS') }}"
