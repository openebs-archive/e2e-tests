apiVersion: v1
metadata:
  name: {{ block_vol_pvc }}
spec:
  volumeMode: Block
  storageClassName: {{ storage_class }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ pvc_size }}